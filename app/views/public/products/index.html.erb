
<div class="container">
    <div class="row">
        <div class="col-md-2">
            <table class="table table-bordered ml-3">
                <thead>
                    <th>ジャンル検索</th>
                </thead>
                <tbody>
                    <% @genres.each do |genre| %>
                    　　<tr>
                          <th><%= genre.name %></th>
                         </tr>
                    <% end %>
                </tbody>
            </table>
        </div>
        <div class="col-10">
            <h2>商品一覧（全<%= @products.count %>件）</h2>
            <div id="container">
        　　　<% @products.each do |product| %>
        　　　<div class="item">
            　　　   <div class="img-area">
            　　　     <%= link_to product_path(product.id) do %>
                     <%= attachment_image_tag product, :image, size: "200" %>
                    <% end %>
                  </div>
                  <div>
                     <%= product.name %>
                  </div>
                  <div>
                     ¥<%= product.price %>
                  </div>
                </div>
        　　　<% end %>
        　　</div>
        　　<%= paginate @products %>
        </div>
    </div>
</div>