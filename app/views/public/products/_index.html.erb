<div class="container">
    <% product.each do |product| %>
        <div class="item">
            <div class="img-area">
                <%= link_to product_path(product.id) do %>
                    <%= attachment_image_tag product, :image, size: "150" %>
                <% end %>
            </div>
            <div><%= product.name %></div>
            <div>¥<%= product.price %></div>
            <div><%= product.genre.name %></div>
        </div>
    <% end %>
</div>
